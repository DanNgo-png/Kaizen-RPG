export function configureSidebar() {
    const configureSidebar = document.getElementById('configure-sidebar');
    if (configureSidebar) {
        configureSidebar.classList.toggle('configure-sidebar-open');
    }
}